---
description: Output response as copyable markdown
model: haiku
---

Output your previous response as copyable markdown.
Wrap it in quadruple backticks (````) so it gets rendered as a code block, and any code blocks inside it are preserved.
